services:
  - type: web
    name: node-sentinel-api
    env: docker
    dockerfilePath: ./Dockerfile
    region: oregon
    plan: free
    envVars:
      - key: KEEP_ALIVE_INTERVAL_SECONDS
        value: "600"
      - key: OCR_ENABLED
        value: "true"
